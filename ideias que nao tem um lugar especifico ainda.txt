-- Remover mais responsabilidade do HTTPRequest, pensar nisso quanto estiver refazendo o fluxo

-- Recriar o HTTPStatus e o HTTPMethod, mas usando a classe e um mapa também, tipo ao invés de ser só um enum, criar os métodos de conversao dentro da própria classe
   Além disso, criar o tipo INVALID para facilitar nos testes e nas checagem do próprio programa.
      Exemplo:
         map[HttpMethod::GET, "GET"]
         map[HttpMethod::POST, "POST"]
         ...


      String methodToString(HTTP::Method){
          return map.get(Method).string();
      }

      HttpMethod stringToMethod(String method){
          String map.get(Method);
          //se nao achou retornar HTTPMelhod::Invalid
      }
    
    ** fazer a mesma coisa para o HTTPStatus